# Experiment HK-ENUM — exact HK partition optimization by enumeration.
instance: spike          # spike | slope | ushape | random
n_prebins: 12            # <= 14 (2^(n-1) contiguous partitions)
kappas: [0.05, 0.1, 0.2, 0.3, 0.5, 0.75, 1.0, 1.5, 2.0, 3.0]
seed: 0
out: outputs/hkenum
hydra:
  job:
    chdir: false
